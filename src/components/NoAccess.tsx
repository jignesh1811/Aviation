import { Typography } from "@mui/material";

const NoAccess: React.FC = () => {
    return (
        <Typography variant="h5" gutterBottom>You dont have access</Typography>
    );
}

export default NoAccess;